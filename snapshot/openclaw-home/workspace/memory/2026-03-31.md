# 2026-03-31
- Tiaro quer migrar a Clara do provedor OpenAI/ChatGPT para Claude no OpenClaw, mantendo a API da OpenAI como fallback.
- Regra operacional confirmada: no OpenClaw, Anthropic/Claude deve ser configurado por `ANTHROPIC_API_KEY` em `/root/.openclaw/secrets.env`; nao ha fluxo OAuth suportado para Anthropic no gateway.
- Estrategia recomendada para fallback: manter `ANTHROPIC_API_KEY` e `OPENAI_API_KEY` ao mesmo tempo no `secrets.env`, apontar `agent-config.yaml` para um modelo `anthropic/...` e preservar uma versao/config alternativa do OpenAI para rollback rapido manual com restart do gateway.
- Contexto do ambiente local do Tiaro (Windows): Git for Windows e Claude Code CLI foram instalados para uso do Claude Desktop/local sessions; a versao validada do CLI foi `2.1.87`.
