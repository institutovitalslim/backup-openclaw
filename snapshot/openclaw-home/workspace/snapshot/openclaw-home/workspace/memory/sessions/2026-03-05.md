## Gateway console access
- Guiado o Tiaro a abrir um túnel SSH local (`ssh -L 18789:127.0.0.1:18789 root@187.77.58.193`) para usar o console web do OpenClaw com segurança.
- Diagnostiquei o erro de autenticação no dashboard e confirmei que o gateway está configurado com `auth.mode = token`; a UI precisa que o token seja copiado do `~/.openclaw/openclaw.json`.
- 19:31 UTC: Instalei e autorizei o `gog` CLI para a conta institutovitalslim@gmail.com usando novo client OAuth Desktop; token salvo no keyring local com senha `gog#2026!Mar` (ainda precisa ser registrada no 1Password e referenciada no `.env`).
